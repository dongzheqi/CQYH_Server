namespace 游戏服务器.网络类
{
	[封包信息描述(来源 = 封包来源.服务器, 编号 = 571, 长度 = 7, 注释 = "祝贺徒弟升级")]
	public sealed class 恭喜徒弟升级 : 游戏封包
	{
		[封包字段描述(下标 = 2, 长度 = 4)]
		public int 徒弟编号;

		// int → byte: 三处描述原本两两打架, 其中两处自洽——封包总长 7, 且本字段 下标=6 长度=1 恰好占满
		// 第 7 个字节; 只有 C# 字段类型写成了 int。按 int 写入时 游戏封包.取字节() 用
		// new MemoryStream(new byte[封包长度]) 建的是 7 字节不可扩容流, 而 int 写入委托要在下标 6 处
		// 写 4 字节(需 10 字节), 一旦有人发送本包即 NotSupportedException。
		// 本包目前全仓无任何发送点(纯埋雷), 故本次修正不改变任何线上行为; 等级值也远小于 255。
		[封包字段描述(下标 = 6, 长度 = 1)]
		public byte 祝贺等级;

		public override ushort 封包编号 => 571;
	}
}
