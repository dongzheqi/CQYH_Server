namespace 游戏服务器.网络类
{
	[封包信息描述(来源 = 封包来源.客户端, 编号 = 619, 长度 = 10, 注释 = "下架寄售物品")]
	public sealed class 下架寄售物品 : 游戏封包
	{
		// 已知不一致, 本次刻意未改: 字段描述写 长度=8, C# 类型却是 uint —— uint 的读取委托
		// (游戏封包.cs) 无视 描述符.长度、固定 ReadUInt32 读 4 字节, 即实际只吃了 下标2~5,
		// 6~9 四个字节被忽略。包总长 10 = 2 + 8 与「长度=8」自洽, 说明原本多半是 long/ulong。
		// 不改的原因: 客户端真实发的是 4 字节还是 8 字节无法在本仓核实; 而小端序下读低 4 字节
		// 对现实量级的订单号结果正确, 现状不会出错。要动需先抓包确认宽度。
		[封包字段描述(下标 = 2, 长度 = 8)]
		public uint 订单编号;

		public override ushort 封包编号 => 619;
	}
}
