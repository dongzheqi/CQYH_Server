namespace 游戏服务器.网络类
{
	[封包信息描述(来源 = 封包来源.客户端, 编号 = 202, 长度 = 6, 注释 = "开始狩猎任务")]
	public sealed class 开始狩猎任务 : 游戏封包
	{
		// 长度 8 → 4: 本包总长 6(包类特性与 封包长度表.cs:2143 两处一致), 载荷自下标 2 起只有 4 字节。
		// 原声明 8 会让 游戏封包.cs:63-79 的 byte[] 读取委托 Seek(2) 后要求 8 字节、而 可用 只有 4,
		// 命中 :76 的 throw new InvalidOperationException("封包字段长度超过剩余流"), 玩家一点「接取狩猎」必断线。
		// 改字段而非改包长: 包长 6 有两个独立来源背书, 且 客户网络.cs:4142 的 handler 根本不读 未知参数,
		// 故本次不改变任何线上字节格式。
		[封包字段描述(下标 = 2, 长度 = 4)]
		public byte[] 未知参数;

		public override ushort 封包编号 => 202;
	}
}
